#env EDITOR=nano crontab -e

find ~/Downloads -mindepth 1 -maxdepth 1 -type f -name *.torrent -exec rm -f {} \;
find ~/.Trash -mindepth 1 -maxdepth 1 -exec rm -rf {} \;
find ~/IdeaProjects -mindepth 1 -maxdepth 2 -type d -name .git -exec sh -c 'TREE=$( echo {} | sed 's_/\.git__g' ); git --git-dir={} --work-tree=$TREE pull' \;